name: publish

# Builds on a version tag and publishes to PyPI through trusted publishing, so no
# token is stored in the repository. The PyPI project has to list this repository,
# this workflow file, and the "pypi" environment as a trusted publisher first.
on:
  push:
    tags: ["v*"]

jobs:
  build:
    # The repository is mirrored to a second account. Only the account registered
    # with PyPI as the trusted publisher may build and publish.
    if: github.repository == 'imaad786/mcp-upload'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Build
        run: |
          python -m pip install --upgrade build
          python -m build
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  publish:
    if: github.repository == 'imaad786/mcp-upload'
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - uses: pypa/gh-action-pypi-publish@release/v1
